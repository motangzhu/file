# 2026-08-31 工作记录
- 回答"一次提问到回答的完整过程是什么"：用两个内联可视化（端到端六阶段链路图、上下文包裹构成结构图）讲清 Agent 的响应机制，重点纠正三个常见误解——模型没有跨轮记忆（靠文件续）、工具返回优先于训练先验、上下文窗口是硬约束。
- 完成《AI-Agent-架构原理详解.html》（469 行 / 30KB，路径 D:/files/WorkBuddy-study/AI-Agent-架构原理详解.html）。结构：Agent 与 Chatbot 分界 → 运行时五层架构 → Agent Loop 五阶段 → 工具调用协议 → 上下文工程 → 记忆系统 → 规划分解 → 可靠性工程 → 完整任务时间线（网关 502 排查案例）→ 失败模式排查表 → 使用者/搭建者双清单。
  - 核心论点：Agent 架构里只有模型层是概率性的，其余四层都是确定性代码；可靠性提升主要靠其余四层约束模型，而非换更强的模型。
  - 文档设计为完整独立 HTML（非 widget 片段），支持 prefers-color-scheme 深浅双主题，左侧 sticky 目录，内联 SVG 用 CSS 变量着色以跟随主题切换。
- 交付形式选择：本工作区既有文档均为 .md，本次详解类内容改用 HTML（含交互与配色分层），若用户后续偏好 .md 需调整。
- 待确认：已两次询问用户对助手的称呼与命名偏好，用户尚未回答，暂未写入身份文件（SOUL/IDENTITY/USER 仍为模板状态，BOOTSTRAP.md 未删除）。
- 完成《AI名词全景词典.html》（路径 D:/files/WorkBuddy-study/AI名词全景词典.html）。背景：用户要求"详细讲解 LLM/MCP 等各类 AI 名词"。此前 show_widget 渲染分层图连续触发 429 限流，本次改为纯静态 HTML 单文件交付（无外部 CDN 依赖），规避限流风险。
  - 结构：顶部"从底层到上层"九层全景图（算力基础设施→模型底座→推理生成→训练对齐→知识增强→工具协议→Agent 编排→应用形态→工程运维），点击层次内名词可定位卡片；下方搜索框（中英文/关键词实时高亮）+ 按层筛选按钮 + 卡片网格。
  - 收录约 47 个核心名词，每个含：中文名、英文、一句话定义、详解、关联概念。深色主题，纯前端 JS 渲染。
  - 复用的可落地经验：当可视化/Widget 工具频繁 429 时，把"要讲的内容"直接产出为自包含 HTML 文件更稳，且同样支持搜索/筛选/跳转。
