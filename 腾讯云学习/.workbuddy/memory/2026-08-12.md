# 2026-08-12 工作日志

## 组长新派任务：CNB 构建配置学习（收费系统项目）
- 目标：熟悉收费系统项目在 CNB（云原生构建）里的构建配置与构建流程，重点是了解 CNB 的 `.cnb.yml` 构建流水线；聚焦三个仓库的流水线配置。
- 方式要求（组长交代）：**只读学习，不要乱执行这几个仓库的构建**；多看脚本、结合之前看的资料、在现项目里找一两个仓库的构建记录（构建历史/日志）研究，便于后续问题排查。
- 已给：CNB `.cnb.yml` 配置结构模型（事件→流水线→stage→script）与学习路线（通读 .cnb.yml→深读 script→对照构建记录→沉淀排障速查表）。
- 进展卡点：用户确认**没有这三个仓库的读权限**，连文件列表/构建页都打不开，所以目前无法直接看 `.cnb.yml` 或构建记录。
- 下一步：需找组长解决——要么给三个仓库加 Reader 权限（只读即可，符合"不要乱执行"要求），要么让组长直接把 `.cnb.yml` 全文 + 关键构建脚本 + 一两个构建日志片段发过来。
- 用户偏好：只给方法、自己画图，不要我代画。
