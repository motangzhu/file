# 8月工作总结

| 项目 | 内容 |
|---|---|
| 汇报人 | 赵子豪 |
| 入职日期 | 2026-08-03 |
| 汇报周期 | 2026-08-03 ~ 2026-08-31 |
| 工作主线 | 云平台与构建体系学习 → 大数据集群与服务跟进 → 发版配置模板化 |

---

## 一、本月工作概述

​		本月为入职首个完整月，整体以**熟悉环境、承接准备**为主。

​		从 8/3 开始看腾讯云产品文档，覆盖 CNB、云原生网关、CFW、WAF、主机安全组、云堡垒机、CEN、NAT、CLB、TDSQL-C MySQL、可观测平台共 11 类；第二周重点看 CNB，并结合 3 个业务仓库的流水线配置了解实际怎么构建。文档学习一直穿插在实际工作里，后面核对服务配置、抽发版模板时都在反复回看。

​		承接范围逐步收敛，由 SaaS 侧转向 TKE 大数据集群（测试 / 生产），大数据内部又从 `datatransfer` 转到拆分出来的 `datasink`（体量更小，先跑通它）。看他们怎么发版时发现，大数据这边没有命令行发版方式，是在 CNB 页面上点按钮发的，所以想照 SaaS 的模式做一套出来，转去做了**发版配置模板化和部署脚本开发**，目前仍在进行。

---

## 二、工作进展

### 2.1 云平台与工具链学习

| 时间 | 学习内容 | 当前程度 |
|---|---|---|
| 8/3 起 | 腾讯云产品文档，覆盖 **CNB、云原生网关、云防火墙 CFW、WAF、主机安全组、云堡垒机、云联网 CEN、NAT 网关、CLB、TDSQL-C MySQL、可观测平台** 共 11 类 | 11 类完成初步浏览，了解各产品定位与控制台入口 |
| 第二周起 | CNB 流水线配置语法、构建触发策略、云开发环境、制品库 | 能读懂现有流水线配置 |
| 8/12 起 | 指定的 **3 个 CNB 仓库**构建流水线配置 | 了解构建阶段划分与镜像制品产出方式；后续发版模板化工作中作为主要参考 |
| 8 月下旬起 | 结合 `datasink` 配置核对与发版模板抽取，回看 CNB 流水线配置与 K8s 部署配置 | 持续进行 |

---

### 2.2 大数据集群与服务跟进

| 时间 | 事项 | 结果 / 当前状态 |
|---|---|---|
| 8/19 | 由 SaaS 侧转入 TKE 大数据集群（测试 / 生产），了解集群拓扑、命名空间划分、工作负载类型；明确目标：**先在测试环境把发版跑通** | 熟悉中 |
| 8/24 | 服务拆分，`datasink` 从 `datatransfer` 中独立，体量更小，按优先级切换为先跑通 `datasink` | 已切换，`datatransfer` 暂缓 |

**当前进展：** 集群还在熟悉阶段，没有正式接管。测试环境发版还没跑通，等模板和脚本做完一起验证；`datatransfer` 暂缓。

---

### 2.3 发版配置模板化与部署脚本开发

**发现的问题：** 看 `datasink` 怎么发版时发现，大数据这边**没有命令行发版方式**，是在 CNB 页面上点按钮发的。SaaS 那边用的是命令行发版，所以想照 SaaS 的模式来，把发版配置抽成模板，再写部署脚本。

**已开展的工作：**

- **抽配置**：把现有配置里跟环境无关的变量抽出来（镜像制品标识、资源规格、副本数、配置映射、健康检查、环境变量等），做成统一模板；
- **整模板**：整理模板目录结构，让 `datatransfer`、`datasink` 两个服务共用一套，不用重复配；
- **写脚本**：写命令行部署脚本，串起 `参数校验 → 配置渲染 → 发布执行 → 结果校验 → 回滚`，和 SaaS 那边的命令行发版保持一致。

**目标发版闭环：**

```
制品标识（digest）→ 发布步骤 → 观测验证 → 回滚条件
```

- **制品标识**：以 digest（sha256 摘要）为准。digest 不可变，能保证跑起来的镜像和构建产物严格一致，不会像 tag 那样被覆盖导致环境不一致；发版记录里留存 digest 与版本映射。
- **发布步骤**：命令行一键执行；
- **观测验证**：发布后校验 Pod 就绪、健康检查通过、日志无异常；
- **回滚条件**：就绪失败 / 健康检查不通过 / 关键指标异常时触发，支持一键回退至上一个 digest。

**当前状态：** 模板和脚本还在写，所以测试环境发版还没跑通，预计 9 月上旬完成。

---

## 三、9月工作计划

| # | 计划事项 | 交付物 / 验收标准 |
|---|---|---|
| 1 | 完成 `datasink` 测试环境命令行发版全流程验证 | 一次完整成功的命令行发版 |
| 2 | 定稿发版配置模板与部署脚本 | 模板目录 + 部署脚本入库，两个服务完成模板接入 |
| 3 | 回滚验证 | 完成 1 次回滚实操，记录回滚耗时 |
| 4 | 把这套命令行发版也用在 `datatransfer` 上 | 两个服务都能用命令行发版 |
